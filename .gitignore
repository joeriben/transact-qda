node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# API keys & local settings
*.key
ai-settings.json

# Uploads
/uploads/
/coach-library/

# ML model cache
/.model-cache/

# Instance-specific branding (operator logo, impressum HTML, etc.)
# Each deployment provides its own files here; public repo keeps it empty.
/static/brand/*
!/static/brand/.gitkeep
!/static/brand/README.md

# PostgreSQL data
/pgdata/
projekte/
backup-*.sql

# DB-Dumps enthalten Nutzerkonten (Hashes), Session-Tokens und Forschungsdaten —
# niemals tracken, auch nicht im privaten Repo.
/backups/

# Dev-only A/B/C/D/E-Forensik-Traces (cname-forensic.ts schreibt pro Run hierher)
/.tmp-forensic-trace/

# Dev-only Critfriend-Batchtests (enthalten echte Forschungsdaten — nie tracken)
/.tmp-critfriend/

# Alles mit .tmp-Präfix ist Wegwerf-Arbeitsstand und enthält regelmäßig echte
# Forschungsdaten.
/.tmp-*/
/.dev-server.log
/logdatei
/HANDOVER.md

# Forschungsmaterial, Datenbank-Abzüge und interne Arbeitsstände gehören nicht
# in dieses Repository. Steht hier als Sperre, nicht weil es erwartet würde.
/backups/
/docs/sessions/
/scripts/strauss-lab/
/eval/
/CLAUDE.md
/.claude/
*.dump
*.sql.gz
pg_dump*
